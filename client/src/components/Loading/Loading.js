import React from "react";

import './Loading.scss';

const Loading = () => (
  <div className="container-loading">
    <span>
      <div className="loading" />
    </span>
  </div>
);
export default Loading;
